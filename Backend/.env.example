# Backend environment variables
# Copy to .env (or export in your shell) before running the backend.

# OpenWeather API key (recommended). If missing/invalid, backend falls back to defaults.
OPENWEATHER_API_KEY=
